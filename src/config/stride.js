'use strict';

/**
 * Stride confirguration
 */
let options = {};

/**
 * Loop interval
 *
 * The interval between each loop counted in seconds.
 */
options['interval'] = 60;

/**
 * Strategy Timeframe
 *
 * The timeframe to run the strategy.
 */
options['strategyTimeframe'] = 'm30';

export default options;
